- block:
  - name: Install and setup fish shell
    pacman:
      name:
        - fish
        - starship
      state: present
  - name: Change shell
    shell: chsh -s "{{ lookup('pipe', 'which fish') }}"
  tags:
  - fish
  - desktop
  - laptop
